import { GoogloginDirective } from './googlogin.directive';

describe('GoogloginDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogloginDirective();
    expect(directive).toBeTruthy();
  });
});
